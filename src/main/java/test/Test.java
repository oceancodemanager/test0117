package test;

public class Test {
	public static void main(String[] args) {
		// Thread
		// ThreadLocal<T>
		// HttpServlet

		// BufferedInputStream
		// DataInputStream
		Integer a = 1;
		Integer b = null;
		if (b instanceof Integer) {
			boolean c = (a == b.intValue());
		}
		if (a.equals(b)) {
			System.out.println("相同");
		} else {
			System.out.println("不相同");
		}
		// ValueStack
		// ActionContext.getContext().getValueStack();
	}
}
